# Core libraries required for the project

numpy>=1.21.0       # For numerical computations and array operations
pandas>=1.3.0       # For data manipulation and preprocessing
matplotlib>=3.4.0   # For data visualization and plotting
scikit-learn>=1.6    # For evaluation, preprocessing data and feature selection

"""
 Save the above content as requirements.txt in your project root directory.
 Install the dependencies by running in your bash:
 pip install -r requirements.txt 
"""
